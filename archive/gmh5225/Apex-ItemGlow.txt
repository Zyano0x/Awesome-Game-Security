Project Path: arc_gmh5225_Apex-ItemGlow_f40sampa

Source Tree:

```txt
arc_gmh5225_Apex-ItemGlow_f40sampa
├── Item Glow.cpp
└── README.md

```

`Item Glow.cpp`:

```cpp
if (MenuSettings::itemwall) {
			for (int i = 0; i < 12000; i++) {
				DWORD64 Entity = GetEntityById(i);
				if (Entity == 0)
					continue;

				Drv.write<int>(Entity + 0x2C0, 1363184265);
			}
		}

```

`README.md`:

```md
# Apex-ItemGlow
Discord Neurosis#0736

```